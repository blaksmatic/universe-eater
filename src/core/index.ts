/**
 * Core — game state, progression, persistence.
 * Import via `import { Game } from '@/core'` in new code; legacy `import from './game'` still works.
 */
export * from '../game';
export * from '../ids';
export * from '../mutators';
export * from '../storage';
export * from '../upgrades';
export * from '../runtime';
